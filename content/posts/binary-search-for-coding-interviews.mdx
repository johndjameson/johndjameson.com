---
date: 2025-07-08
description: Binary search is the best. It’s fast and easy to write in coding interviews. Here are my go-to variants written in TypeScript.
title: Binary Search for Coding Interviews
titleSize: 12.4
---



## The short version

The most compact, dependable version of quicksort I know is this:

```typescript
const binarySearch = (array: number[], target: number): number => {
  let leftIndex = 0;
  let rightIndex = array.length - 1;

  while (leftIndex <= rightIndex) {
    const midIndex = Math.floor((leftIndex + rightIndex) / 2);

    if (array[midIndex] === target) return midIndex;

    if (array[midIndex] < target) {
      leftIndex = midIndex + 1;
    } else {
      rightIndex = midIndex - 1;
    }
  }

  return -1;
};
```

## Shortest version

```typescript
function bs(a: number[], t: number) {
  let l = 0, r = a.length - 1;
  while (l <= r) {
    let m = (l + r) >> 1;
    if (a[m] === t) return m;
    a[m] < t ? l = m + 1 : r = m - 1;
  }
  return -1;
}
```