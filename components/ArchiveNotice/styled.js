import styled from 'styled-components';
import { rgba } from 'polished';

export const ArchiveNoticeBase = styled.p`
  background-color: ${rgba('red', 0.15)};
  color: #e93434;
  font-size: 14px;
  padding: 20px;
`;
