import styled from 'styled-components'
import { rgba } from 'polished'

const ArchiveNotice = styled.p`
  background-color: ${rgba('red', 0.15)};
  color: #e93434;
  padding: 20px;
`

export default ArchiveNotice
