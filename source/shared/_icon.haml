- name   ||= ''
- size   ||= 40
- width  ||= size
- height ||= size

- unless defined? text
  %span.srt= name.humanize

%svg.icon{ width: width, height: height }
  %use{ 'xlink:href' => "#icon-#{name}" }
