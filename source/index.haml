---
title: Home
description: "Front-end Developer at Code School"
---

%section.row
  .cell.well
    .g.mtm
      .g-b.g-b--m--1of2
        %h1.mbs Hey, there.
        :markdown
          I&rsquo;m John D. Jameson&mdash;**front‑end developer** and dedicated
          indoorsman in Orlando, Florida.
        %p
          I work (and learn) at
          = link_to 'Code School', 'https://www.codeschool.com/', class: 'link link--bordered link--inverted'
          , geek out over
          = link_to 'CSS architecture', 'http://mvcss.io/', class: 'link link--bordered link--inverted'
          , and love building tools for people on the web.

%section.row
  .cell.cell--s.well.well--l

    - post = get_posts().first
    = link_to post.data.title, post.url, class: 'h2 tfb'
    %time.label.db.mbm= pretty_date(post.data.date)
    %p.mbl= post.data.description

    .card

      %h2.mbs Blog Posts
      %ul.list.list--divided
        - get_posts().drop(1).each do |post|
          %li.list-item.split.split--responsive
            .split-cell.prs
              = link_to post.data.title, post.url, class: 'h3 tfb'
            .split-cell
              %time.label= pretty_date(post.data.date)
