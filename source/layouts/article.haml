!!! 5
%html{ :lang => 'en' }
  = partial 'shared/head'

  %body{ class: page_classes }

    = partial 'shared/header'

    %section.row.row--a
      .cell.well.well--l
        .g
          .g-b.g-b--center.g-b--m--3of4
            .bchb.pal.tac.tci
              %h1= current_page.data.title
              %time.tsm.ttu.twb= pretty_date(current_page.data.date)

    %section.row
      .cell.well.well--l
        .g
          .g-b.g-b--center.g-b--m--3of4
            %article.post
              = find_and_preserve do
                = yield

    = partial 'shared/footer'
